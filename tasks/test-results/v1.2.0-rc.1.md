# Pearl Relay v1.2.0-rc.1 候选验证报告

## 当前状态

状态：**本地、CI 与隔离服务器 PASS；等待最终报告提交的 CI、tag 和
GitHub prerelease**。

本报告随候选源码维护。只有同一最终提交的本地门禁、GitHub Actions、
隔离服务器和发布资产身份全部一致后，才会改为 PASS 并创建
`v1.2.0-rc.1` annotated tag。

## 候选范围

- 玩家通过 `/pearlrelay save <名称>` 采集当前维度、脚部精确位置和视线
  命中点。
- 完整参数保存形式和 schema v2 保持兼容。
- 配置使用原子替换、单一最近成功备份、严格整文件校验和同 UUID 安全恢复。
- 生命周期压力门覆盖阶段异常及 shutdown/cleanup 竞态。

## 身份

- 分支：`v1.2-player-snapshot-save`
- 版本：`1.2.0-rc.1`
- tag：尚未创建
- 最终候选提交：待记录
- 已完成实现、RC 构建和隔离服验证的源码提交：
  `176d7dd97605f46c53cc5c7374101d10b6dbb21e`
- 文件：`pearlrelay-1.2.0-rc.1.jar`
- CI 实测 JAR SHA-256：
  `98507f700eb534563c5e973e2262421b43fb9fa4e0a19e0f4bdb0c2f447b2a3f`
- 隔离服务器 JAR SHA-256：
  `98507f700eb534563c5e973e2262421b43fb9fa4e0a19e0f4bdb0c2f447b2a3f`

## 必需门槛

- [x] `./gradlew --no-daemon clean test build`
- [x] 开发客户端三个场景各 20/20
- [x] 生产 JAR 客户端三个场景各 20/20
- [x] GitHub Actions `Verify` 和 `build`
- [x] CI 产物的提交 SHA、JAR、`fabric.mod.json` 和 `SHA256SUMS` 一致
- [x] 隔离服务器启动、命令、配置恢复、重复重启和孤儿检查
- [x] 隔离服和生产服最终均停止
- [ ] annotated tag、GitHub prerelease 与下载资产复核

## 本地自动化

环境：

- macOS aarch64
- Eclipse Temurin Java 25
- Gradle `9.5.1`
- Fabric Loom `1.17.0-alpha.7`
- 构建 Fabric API `0.152.2+26.2`
- Carpet `26.2+v260616`

结果：

- `./gradlew --no-daemon clean test build`：PASS，1 分 20 秒。
- JUnit：56/56，0 failure、0 error。
- Fabric 服务端 GameTest：25/25。
- 固定种子生命周期门：1,000 次混合阶段执行和 256 次
  shutdown/cleanup 交错通过；最终 0 active execution、0 假人。
- `./gradlew --no-daemon runClientGameTest`：PASS，4 分 51 秒。
- `./gradlew --no-daemon runProductionClientGameTest`：PASS，4 分 56 秒。
- 开发与生产 JAR 客户端的本人投珠、原版滞留/释放、玩家快照保存与点火
  传送三个场景各 20/20。
- 两个客户端运行各保存 6 张截图：本人珍珠、原版滞留、快照保存、点火前、
  假人可见和传送后。
- 生产客户端日志包含 20 次快照保存和 20 个不同 execution ID 的完成终态，
  没有 crash report。
- 生产 `CodeSource` 强制断言通过；主类来自普通发布 JAR，且 JAR 不含测试
  包、夹具或 gametest 元数据。

本地候选 JAR：

- 文件：`build/libs/pearlrelay-1.2.0-rc.1.jar`
- 大小：`78,253` bytes
- 测试前后 SHA-256：
  `112e756bb0e01eb7583769f5adc96e4b2c6548373459bbcf4fd2931f268ec6fa`
- `fabric.mod.json`：版本 `1.2.0-rc.1`、环境 `server`，Minecraft
  `26.2`、Java `>=25`、Fabric Loader `>=0.19.3`、Fabric API
  `>=0.152.1+26.2`、Carpet `>=26.2`。

已知测试环境噪声：

- 新 GameTest 运行目录最初没有 `server.properties` 和 `eula.txt`，随后由
  Fabric GameTest 正常接管。
- 离线测试账号产生 Mojang/Realms 401/404，macOS Metal/OpenGL 产生驱动
  警告；均没有对应断言失败。
- Gradle 报告 Gradle 10 弃用提示；本次使用 Gradle 9.5.1 成功完成。

## GitHub Actions 与 CI 产物

- [`Verify` run 30364739513](https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30364739513)：
  PASS。
  - Java 25 clean build、JUnit、服务端 GameTest 与产物校验：52 秒。
  - Java 25 XVFB 生产客户端 GameTest：4 分 57 秒。
- [`build` run 30364741058](https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30364741058)：
  PASS。
- CI `COMMIT_SHA`：
  `176d7dd97605f46c53cc5c7374101d10b6dbb21e`。
- 生产客户端实测候选产物：
  `production-client-candidate-176d7dd97605f46c53cc5c7374101d10b6dbb21e`。
- 持久客户端证据：
  `production-client-evidence-176d7dd97605f46c53cc5c7374101d10b6dbb21e`。
- CI JAR 为 `78,253` bytes；内嵌 `fabric.mod.json` 为
  `1.2.0-rc.1`；`SHA256SUMS` 自检通过。
- CI 日志包含 20 次简写保存、20 个不同 execution ID 的完成终态；上传
  6 张截图且无 crash report。

## 部署边界与回滚

- 生产根目录：`/opt/minecraft1.21.11`
- 生产基线：`pearlrelay-1.1.0.jar`
- 生产基线 SHA-256：
  `a15cf782a7be0bfd2aa4c7c287fa51beb9eb10026c3ca88c34b5aec2d64166a9`
- 隔离测试根目录：`/opt/minecraft-pearlrelay-test`
- 隔离服部署前版本：`pearlrelay-1.1.0-rc.2.jar`
- 隔离服部署前 SHA-256：
  `7fe3ee64fbc11cdab5af2abb1487e01c63fbaaaebbabe855a2b1889af457909b`
- 服务器初始状态：生产服与隔离服均停止，端口 `25565`/`25566` 均未监听。
- RC 只部署隔离服；生产 JAR、世界、配置和启动状态不变。

## 隔离服务器验证

环境：

- 根目录：`/opt/minecraft-pearlrelay-test`
- 监听：仅 `127.0.0.1:25566`
- Java：25
- Minecraft：26.2
- Fabric Loader：`0.19.3`
- Fabric API：`0.152.1+26.2`
- Carpet：`26.2+v260616`
- Pearl Relay：`1.2.0-rc.1`
- RC2 回滚文件：
  `/opt/minecraft-pearlrelay-test/backups/pearlrelay-1.1.0-rc.2-pre-v1.2.0-rc.1-176d7dd-20260728.jar`

部署与启动：

- 只上传到 `incoming/`，远端 SHA-256 与 CI 完全一致后才切换 `mods/`。
- 第一次启动检查错误复用了旧 `latest.log` 的 `Done` 行；当时未发送任何
  测试命令。随后改为同时要求本次日志时间、当前进程和
  `127.0.0.1:25566` 监听，确认实际启动健康。
- 后续两次重启在启动前保留旧 `latest.log`，只用本次新日志和当前端口判定
  就绪，没有再次命中旧日志。
- `pearlrelay test` 两轮均返回 `Pearl Relay command works.`。

配置恢复：

- 测试假人 `rc12valid` 站在夹具位置、水平朝南，通过简写命令两次保存
  `rc_valid`，目标为 `minecraft:stone@0,101,3`，生成同 UUID 主文件和
  有效 `.bak`。
- 用固定无效 JSON 替换该测试主文件后，第一次 `list` 只记录一次
  `CONFIG_RECOVERED_RETRY`；主文件恢复为备份哈希，损坏内容完整保存在
  `.corrupt-1`。
- 第二次 `list` 成功列出 `rc_valid`；重启后再次列出成功，没有重复恢复。
- 第二个测试假人 `rc12broken` 只保存一次，因此没有 `.bak`。替换为同一
  无效 JSON 后，`list` 返回 `CONFIG_CORRUPT`；主文件保持无效夹具哈希，
  没有生成 `.bak` 或 `.corrupt-1`，其他 UUID 文件未改变。

清理与重启：

- 两个测试假人均通过 Carpet `kill` 退出；每次停止前和重启后在线玩家为 0。
- 完成两次正常 `stop -> start` 循环；每次均加载 `1.2.0-rc.1`，无孤儿
  假人、无 crash report、无启动致命错误。
- 验证结束后隔离服正常保存并停止；生产服始终未启动。
- 最终 `25565` 与 `25566` 均未监听，没有 screen 会话或 Minecraft Java
  进程。
- 生产 JAR 保持 `pearlrelay-1.1.0.jar`，SHA-256 仍为
  `a15cf782a7be0bfd2aa4c7c287fa51beb9eb10026c3ca88c34b5aec2d64166a9`。

## 尚待记录

- 最终报告提交的 GitHub Actions 与 CI JAR 字节一致性。
- tag、Release URL、发布资产大小和下载复核哈希。
