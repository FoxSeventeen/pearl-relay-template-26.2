# Pearl Relay v1.2.0 正式候选验证报告

## 发布结论

状态：**项目所有者已批准 RC；正式候选门禁执行中。**

本报告随正式候选源码提交保存。只有本报告所在提交的本地门禁、GitHub
Actions、生产 JAR 身份和代码审查全部通过后，才创建 annotated
`v1.2.0` tag 和正式 GitHub Release。

本次稳定版不增加 RC 之后的生产逻辑或依赖，也不声明指定整合包、代理、
权限系统兼容性或主观体验承诺。真人机械回归继续按已批准边界暂缓；日后
统一实机测试的全部玩家可见行为见
[`v1.2.0-rc.1` 报告](v1.2.0-rc.1.md#日后玩家统一实机测试清单)。

## 候选身份

- 版本：`1.2.0`
- tag：`v1.2.0`
- tag 目标：本报告完成后的正式候选提交
- 文件：`pearlrelay-1.2.0.jar`
- Git 作者与 tagger：`FoxSeventeen <1661863496@qq.com>`
- 正式 JAR SHA-256：等待最终候选 CI
- 正式资产来源：最终候选 GitHub Actions
  `production-client-candidate-<COMMIT_SHA>` 中完成客户端门禁的 JAR

## 必需门槛

```shell
./gradlew --no-daemon clean test build
./gradlew --no-daemon runClientGameTest
./gradlew --no-daemon runProductionClientGameTest
```

必须满足：

- JUnit 56/56、Fabric 服务端 GameTest 25/25；
- 开发态客户端三个场景各 20/20；
- 生产发布 JAR 客户端三个场景各 20/20；
- 固定种子生命周期 1,000 次执行及 256 次 shutdown/cleanup 交错；
- 生产主类 `CodeSource` 来自普通发布 JAR；
- 发布 JAR 在生产客户端任务前后 SHA-256 不变；
- 发布 JAR 不包含测试代码、夹具或 GameTest 元数据；
- 最终候选、tag、main 的 Java 25 build 和生产客户端 XVFB 作业均绿色；
- `COMMIT_SHA`、候选 JAR、`SHA256SUMS` 和 Release 资产完全一致。

## RC 已完成证据

- `v1.2.0-rc.1` 的本地、分支 CI、标签 CI 和公开 Release 均 PASS。
- 同一 RC JAR 已在隔离 Minecraft 26.2 服务端验证简写保存、有效备份恢复、
  无备份损坏拒绝、两次正常重启和零孤儿假人。
- RC 隔离服实测 JAR SHA-256：
  `98507f700eb534563c5e973e2262421b43fb9fa4e0a19e0f4bdb0c2f447b2a3f`。
- RC 验证后生产服与隔离服均停止；生产服始终使用 `v1.1.0`。
- 详细证据见 [`v1.2.0-rc.1` 报告](v1.2.0-rc.1.md)。

## 正式候选结果

- 本地 `./gradlew --no-daemon clean test build`：PASS，1 分 14 秒；
  JUnit 56/56、Fabric 服务端 GameTest 25/25。
- 本地候选 JAR：`78,249` bytes，SHA-256
  `ca0ba76a27eb58b8ee6ffc176bd9ab596c58a6b3b4dead167ca7618fb546579b`。
- 本地 JAR 的 `fabric.mod.json` 为 `1.2.0`、环境 `server`，依赖约束正确，
  且不含测试代码、夹具或 GameTest 元数据。
- 本地开发客户端 GameTest：等待执行
- 本地生产 JAR 客户端 GameTest：等待执行
- 最终候选分支 CI：等待执行
- 标签 CI：等待执行
- 正式 Release 资产下载复核：等待执行
- `main` 快进合入与 CI：等待执行

## 代码审查

- RC 五轴审查：APPROVE，无阻断、高风险或中风险发现。
- 正式版相对 RC 只允许版本、文档和发布记录变化；将在合入前复核。
- 新增生产依赖：无。
- 发布材料不得包含服务器密码、账号凭据、私有配置或未脱敏日志。

## 服务器与回滚边界

- 本次授权包含稳定版发布和合入 `main`，**不包含生产服务器部署**。
- 生产目录继续保留 `pearlrelay-1.1.0.jar`，服务器保持停止。
- 隔离服继续保留 `pearlrelay-1.2.0-rc.1.jar` 和已验证的 RC2 回滚副本，
  服务器保持停止。
- 只有项目所有者再次明确授权生产部署后，才上传正式 JAR、创建新的
  `v1.1.0` 时间戳回滚副本并切换生产 `mods/`。

## 最终发布材料

- annotated `v1.2.0` tag
- `pearlrelay-1.2.0.jar`
- `SHA256SUMS`
- 本报告
- GitHub Actions build、production-client candidate 和客户端证据
