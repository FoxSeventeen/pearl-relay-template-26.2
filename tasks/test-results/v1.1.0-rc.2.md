# Pearl Relay v1.1.0-rc.2 自动化与无客户端测试报告

## 结论

**PASS（自动化与无客户端范围）**。

候选包已通过 Java 25 clean build、47 个 JUnit、21 个 Fabric GameTest、
GitHub Actions 质量门，以及 Minecraft 26.2 隔离服务端上的部署、命令、
未加载区块拒绝、持久化、清理和重启检查。代码审查未发现高严重度问题。

本报告初版仍把 16 组行为列为玩家实机门槛。2026-07-28 完成的客户端
E2E Checkpoint C 已补齐真实装置传送、玩家可见生命周期和生产 JAR 客户端
证据；该旧门槛已由修订后的
[`docs/testing/v1.1.0-player-acceptance.md`](../../docs/testing/v1.1.0-player-acceptance.md)
取代，不再要求逐项真人重放。

## 后续客户端 E2E 证据

从 RC2 已测试生产代码提交
`ff2875ec8758001fc01aa48b23b3dd94f28ba483` 到客户端证据提交
`d749d64e998f593e4e9f3f08bfe5880e3e694ce1`，`src/main/` 和
`gradle.properties` 没有变化；后续提交只增加测试、构建门槛和文档。

Checkpoint C 结果：

- 47 个 JUnit 和 21 个 Fabric 服务端 GameTest 继续通过；
- 开发态客户端三个场景各 20 轮通过；
- 生产 JAR 客户端三个场景各 20 轮通过；
- 真实客户端投掷自己的珍珠，经原版气泡柱滞留装置由 Pearl Relay 释放，
  客户端和服务端玩家均到达目的地区域；
- 假玩家名称、类型、位置、朝向、一次使用、双端可见和最终移除均自动断言；
- GitHub Actions Java 25 + XVFB 生产客户端作业绿色完成并上传日志和截图；
- 被测和上传的 CI JAR SHA-256 均为
  `dd308aa436285e670b88319290c47814da03577298ae2b0dd108806536d595e2`。

完整证据见
[`tasks/test-results/client-e2e-phase3.md`](client-e2e-phase3.md)。

## 候选标识

- 分支：`headless-hardening`
- 已测试生产代码提交：
  `ff2875ec8758001fc01aa48b23b3dd94f28ba483`
- 模组版本：`1.1.0-rc.2`
- 文件：`pearlrelay-1.1.0-rc.2.jar`
- 本地构建与远端隔离服 SHA-256：
  `7fe3ee64fbc11cdab5af2abb1487e01c63fbaaaebbabe855a2b1889af457909b`
- Fabric 元数据依赖：
  - Minecraft `26.2`
  - Java `>=25`
  - Fabric Loader `>=0.19.3`
  - Fabric API `>=0.152.1+26.2`
  - Carpet `>=26.2`

发布 JAR 已检查，不包含 GameTest、测试类或测试资源。

## 本地自动化结果

环境：

- macOS aarch64
- Eclipse Temurin `25.0.3+9-LTS`
- Gradle `9.5.1`
- Fabric Loom `1.17.0-alpha.7`
- 构建 Fabric API `0.152.2+26.2`
- Carpet `26.2+v260616`

执行：

```shell
GRADLE_USER_HOME=/private/tmp/pearlrelay-gradle-home \
JAVA_HOME=/private/tmp/temurin-jdk25/Contents/Home \
GRADLE_OPTS='-Djava.net.preferIPv4Stack=true' \
./gradlew --no-daemon clean test build
```

结果：

- `BUILD SUCCESSFUL`
- JUnit：47/47 通过，0 failure、0 error、0 skipped
- Fabric GameTest：21/21 通过
- GameTest 框架启动时读取不到临时 `server.properties` 和 `eula.txt`，
  随后由 Fabric GameTest 正常接管；这两条启动输出不是候选失败。
- 当前 Gradle 工具链仍报告 Gradle 10 弃用提示，不影响本次 Gradle
  9.5.1 构建。

## GitHub Actions

- 客户端 E2E Checkpoint C：
  <https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30335238993>
- RC2 Verify #4：
  <https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30323925735>
- RC2 现有 build #21：
  <https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30323925729>
- 质量门首次成功证明：
  <https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30322852891>
- 临时破坏断言后的预期失败证明：
  <https://github.com/FoxSeventeen/pearl-relay-template-26.2/actions/runs/30323090685>

临时失败分支已删除，错误断言从未进入 `headless-hardening`。工作流不包含
服务器密码、IP 凭据或部署步骤。

## 命令级 GameTest 矩阵

测试通过真实 Brigadier dispatcher，以两个独立 UUID 的注册
`ServerPlayer` 执行命令并捕获玩家消息、补全、日志、实体和交互状态。

| 行为 | 结果 | 自动断言 |
|---|---|---|
| `test/save/list/remove` | PASS | 返回值、消息、持久化与删除结果 |
| 双 UUID 列表、补全、触发隔离 | PASS | 对方名称不可见、不可补全、触发为 `RELAY_NOT_FOUND` |
| v1 旧配置 | PASS | 可列出；触发为 `RELAY_REQUIRES_RESAVE`；同名保存升级 v2 |
| 0 颗本人珍珠 | PASS | `OWNED_PEARL_NOT_FOUND`，0 假人、0 使用 |
| 只有他人珍珠 | PASS | 不计入本人，0 假人、0 使用 |
| 1 颗本人珍珠 | PASS | 一次接受、一次点按、同 ID 唯一终态、最终清理 |
| 多颗本人珍珠 | PASS | 允许触发但仍只点按一次，不选择或移动珍珠 |
| 快速重复触发 | PASS | 第二次为 `EXECUTION_ALREADY_ACTIVE`，不生成第二个假人 |
| 真实未加载目标区块 | PASS | `TARGET_CHUNK_UNLOADED`，仅一条 rejected，无 accepted/terminal |

## 无加载副作用证据边界

### 真实 Minecraft 服务端证据

Fabric GameTest 在实际 `ServerLevel` 与命令分发器上记录触发前后的
`TicketStorage` 快照、entity-ticking readiness、`getChunkNow`、假人数和
音符盒交互次数。未加载目标的拒绝满足：

- `TicketStorage` 快照完全相同；
- 触发前后都不是 entity-ticking；
- 触发前后 `getChunkNow` 都为 `null`；
- 0 个中继假人、0 次点按；
- 恰好一条 `rejected`，没有 `accepted` 或 `terminal`。

隔离服还在真实 Minecraft 26.2 世界中复核了 `rc_unloaded`：
返回 `TARGET_CHUNK_UNLOADED`，随后区块 `[100, 0]` 仍未加载、未标记
force-load，在线玩家为 0。

### 合成 `WorldView` 证据

单元测试用带调用计数器的 `WorldView` 覆盖：

- 保存目标区块卸载；
- 假人出生碰撞箱跨入卸载邻区块；
- 眼睛到目标的路径跨入卸载邻区块；
- 正、负坐标区块边界；
- `getChunkNow` 可见但不能 entity tick 的 FULL 邻区块。

这些测试证明相关拒绝发生在 `blockId`、碰撞、raycast、珍珠扫描和玩家占位
检查之前。它们不能单独证明 Minecraft 内部票据行为，因此票据结论只使用
上一节真实服务端 GameTest。

## 隔离服务端结果

- 根目录：`/opt/minecraft-pearlrelay-test`
- 监听范围：仅 `127.0.0.1:25566`
- Java：25
- Minecraft：26.2
- Fabric Loader：0.19.3
- Fabric API：`0.152.1+26.2`
- Carpet：`26.2+v260616`
- Pearl Relay：`1.1.0-rc.2`
- RC1 回滚备份：
  `/opt/minecraft-pearlrelay-test/backups/pearlrelay-1.1.0-rc.1-pre-rc2-ff2875e.jar`

远端上传后先核对 SHA-256，再切换 JAR。RC2 健康启动、命令入口和既有配置
列表通过；正常重启后配置仍可列出，测试假人清理后为 0 玩家，未生成崩溃
报告。

首次自动重启脚本错误复用了上一轮 `latest.log` 的就绪标记，因此过早发送
了五条控制台命令，服务端记录五条通用“命令执行异常”。该问题属于测试
控制脚本的启动竞态；等待新一轮服务端完全就绪后逐条重试全部通过，模组
未崩溃且世界正常保存。

验证结束后隔离服已正常保存并停止。生产服务始终为 inactive，生产端口
`25565` 和隔离端口 `25566` 最终都未监听；生产目录和生产世界未修改。

## 发布门槛

- 自动化与无客户端门槛：PASS
- 真实客户端 E2E 与生产 JAR 客户端门槛：PASS
- 原 16 项玩家行为的自动化证据映射：PASS
- 双原生客户端决策：当前不需要；双 UUID 命令 GameTest 覆盖服务端隔离边界
- 代码审查高严重度问题：0
- Task 1–4：完成
- Task 5（配置原子写入/损坏恢复）：尚未执行
- Task 6（生命周期压力/故障注入）：尚未执行
- 修订后的发布门槛：项目所有者已于 2026-07-28 批准
- `v1.1.0` 稳定版：仍需在最终候选提交上重跑完整自动化并作最终发布决定；
  只有声明指定整合环境或主观体验承诺时才增加一次短小探索性检查
