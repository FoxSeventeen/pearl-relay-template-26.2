# Pearl Relay v1.1.0 正式候选验证报告

## 发布结论

状态：**正式候选验收 PASS**。

本报告随正式候选源码提交保存，记录可在提交内复现的本地门槛、发布 JAR
身份检查和代码审查。`v1.1.0` tag 只能在本报告所在提交的 GitHub Actions
全部绿色后创建；Release、资产摘要和服务器静态部署结果由 GitHub Release
及发布后的远端核验记录提供。

本次 Release 不声明特定整合包、代理、权限系统兼容性或主观体验承诺。
依据项目所有者于 2026-07-28 批准的
[`docs/testing/v1.1.0-player-acceptance.md`](../../docs/testing/v1.1.0-player-acceptance.md)，
原 16 项机械行为以自动化和隔离服务端证据验收，不增加重复真人测试。

## 候选身份

- 版本：`1.1.0`
- tag：`v1.1.0`
- tag 目标：本报告所在的正式候选提交
- 文件：`pearlrelay-1.1.0.jar`
- 正式 Release JAR SHA-256：
  `a15cf782a7be0bfd2aa4c7c287fa51beb9eb10026c3ca88c34b5aec2d64166a9`
- 正式资产来源：GitHub Actions
  `production-client-candidate-<COMMIT_SHA>` 中完成客户端门禁的 JAR
- JAR 大小：`68,857` bytes
- Git 作者与 tagger：`FoxSeventeen <1661863496@qq.com>`

## 必需门槛

```shell
./gradlew --no-daemon clean test build
./gradlew --no-daemon runClientGameTest
./gradlew --no-daemon runProductionClientGameTest
```

必须满足：

- JUnit 47/47、Fabric 服务端 GameTest 21/21；
- 开发态客户端三个场景各 20 轮；
- 生产发布 JAR 客户端三个场景各 20 轮；
- 生产主类的 `CodeSource` 来自普通发布 JAR；
- 发布 JAR 在生产客户端任务前后 SHA-256 不变；
- 发布 JAR 不包含测试包、夹具或 gametest 元数据；
- GitHub Actions 的 Java 25 build 与生产客户端 XVFB 作业均绿色；
- CI 中 `COMMIT_SHA`、候选 JAR、`SHA256SUMS` 与证据产物一致。

## 本地结果

- `./gradlew --no-daemon clean test build`：PASS，2 分 5 秒；
  JUnit 47/47、Fabric 服务端 GameTest 21/21。
- `./gradlew --no-daemon runClientGameTest`：PASS，5 分 18 秒；
  客户端本人珍珠投掷、原版滞留/释放、Pearl Relay 完整触发三个场景各
  20/20。
- `./gradlew --no-daemon runProductionClientGameTest`：PASS，6 分 56 秒；
  同样三个场景各 20/20，并在 Pearl Relay 场景中记录 20 个唯一 completed
  终态、20 次假玩家执行后退出。
- 本地 macOS 门禁 JAR SHA-256：
  `31c26706ba085b469b3106adc0ec4f74e121cc1e7be8626ce82a39ff38588daf`。
  它与 Linux CI JAR 的所有条目内容、CRC、压缩大小和总大小一致；整包
  SHA 差异只来自 ZIP 固定时间戳的时区编码。正式发布和部署只使用上方
  CI 实测 JAR。
- 生产主类 `CodeSource` 的强制断言通过，来源是普通发布 JAR，不是开发
  classes 目录或 `pearlrelay-gametest` JAR。
- 生产客户端任务前后 JAR SHA-256 保持一致；发布 JAR 中无
  `pearlrelay/test`、`pearlrelay-gametest` 或测试元数据。
- `fabric.mod.json` 为 `1.1.0`，环境为 `server`，Minecraft `26.2`、
  Java 25、Fabric API 和 Carpet 依赖约束正确。
- 生产 JAR 客户端门禁保存 5 张关键截图：本人珍珠、原版滞留、触发前、
  假玩家可见、传送后。

日志中的离线测试账号 401/404、Realms/公钥请求、Metal shader 警告和
`Can't keep up` 属于隔离测试环境噪声；没有断言失败、客户端崩溃或残留
测试服务端。

## 自动化证据范围

- 命令、配置、补全与双 UUID 隔离；
- 全部稳定失败码和玩家消息；
- 未加载区块的真实票据、readiness、实体和交互无副作用；
- 原版气泡柱珍珠滞留与释放；
- 真实连接客户端投掷本人珍珠并由 Pearl Relay 完成传送；
- 假玩家名称、位置、朝向、一次使用、客户端可见和最终移除；
- 接受、拒绝、失败、超时、重复触发和 shutdown 的唯一终态与清理。

详细映射见：

- [`docs/testing/v1.1.0-player-acceptance.md`](../../docs/testing/v1.1.0-player-acceptance.md)
- [`tasks/test-results/client-e2e-phase3.md`](client-e2e-phase3.md)
- [`tasks/test-results/v1.1.0-rc.2.md`](v1.1.0-rc.2.md)

## 代码审查

- 正确性、可读性、架构、安全和性能：**APPROVE，无阻断项**
- 相对 `v1.1.0-rc.2` 的生产代码变化：无
- RC2 之后的实现变化仅限真实客户端测试支持、发布 JAR 来源断言、CI 门槛
  和文档
- 新增生产依赖：无
- 生产行为相对 RC2：无变化
- `git diff --check`：PASS
- 生产源码 `TODO/FIXME/HACK/XXX` 扫描：无结果
- 发布材料凭据与私有地址扫描：无结果
- 发布材料中的服务器密码、账号凭据和私有配置：不得包含

## 部署与回滚

- 目标：`/opt/minecraft1.21.11/mods/pearlrelay-1.1.0.jar`
- 部署后状态：保持停止，不启动 Minecraft，不加载生产世界
- 部署前必须记录当前 `1.0.0` JAR SHA-256，并保存时间戳回滚副本
- 回滚：在停服状态下恢复备份的 `pearlrelay-1.0.0.jar`

## 最终发布材料

- annotated `v1.1.0` tag
- `pearlrelay-1.1.0.jar`
- `SHA256SUMS`
- 本报告
- GitHub Actions build 与 production-client evidence
