# Changelog

本项目从 `1.1.0-rc.1` 开始维护发布记录。

## [Unreleased]

暂无。

## [1.2.0-rc.1] - 2026-07-28

### Added

- 增加推荐的 `/pearlrelay save <名称>` 玩家快照形式，自动采集当前维度、
  脚部精确位置和实际视线命中点。
- 增加同目录临时写入、文件刷新、原子替换、最近成功 `.bak` 和可追溯的
  `.corrupt-N` 配置恢复。
- 增加配置写入故障注入、严格整文件校验、固定种子生命周期压力测试，以及
  玩家快照保存的命令级和真实客户端端到端覆盖。

### Changed

- 完整参数 `/pearlrelay save <名称> <维度> <出生坐标> <看向坐标>` 继续
  兼容；简写与完整形式共享同一目标解析、安全拒绝和 schema v2。
- 保存时允许命令玩家本人站在未来假人位置；点火时该位置若仍被任何玩家
  占据，仍会在生成假人前拒绝。
- 配置的任一 JSON、schema 或中继字段损坏都会使整份玩家配置失败，不再
  静默过滤无效条目。

### Fixed

- 直接覆盖配置文件时，进程或文件系统故障可能留下半写主文件的问题。
- 配置恢复可能继续执行当前业务动作、覆盖有价值的损坏正文或跨玩家使用
  备份的风险。
- 玩家连续以相同视角保存时，客户端测试可能没有把最新朝向同步给服务端的
  证据缺口。

### Migration

- `1.1.0` 的 schema v2 配置可直接使用，不需要迁移。
- schema v1 配置仍可列出；玩家可站到装置点、看向点火方块并执行同名
  `/pearlrelay save <名称>` 完成升级。
- 主配置损坏且同玩家备份有效时，第一次命令返回
  `CONFIG_RECOVERED_RETRY`；重试后才执行保存、删除或点火。

### Validation

- 候选提交、JAR、CI、隔离服和发布资产的最终证据见
  `tasks/test-results/v1.2.0-rc.1.md`。

## [1.1.0] - 2026-07-28

### Added

- 为命名中继增加保存时目标指纹和触发前的维度、区块、出生空间、射线、
  方块类型与执行者珍珠所有权预检。
- 增加带唯一执行 ID、超时和幂等清理的 Carpet 假玩家执行生命周期，以及
  `accepted`、`rejected`、`terminal` 结构化日志。
- 增加 Java 25 JUnit、Fabric 服务端 GameTest、真实 Fabric Client
  GameTest、生产发布 JAR Client GameTest 和隔离服务端验证门槛。

### Changed

- 有效触发固定执行一次 `spawn -> lookAt -> use -> cleanup`；模组只检查珍珠
  是否就绪，不选择、移动或直接释放任何珍珠。
- 未达到实体 tick 状态的目标、出生范围或射线路径区块直接拒绝，不自动
  加载或生成区块。
- 配置升级为按玩家 UUID 隔离的 `schemaVersion: 2`，记录目标方块坐标和
  注册表 ID；旧配置仍可列出，但需要同名重存后触发。
- 用真实 Fabric Client GameTest、生产发布 JAR Client GameTest 和隔离服务端
  证据替代原 16 项强制玩家回归。
- 日常开发、PR 和 RC 不再要求真人重复机械验收；只有声明指定整合环境或
  主观体验承诺时，稳定版前才运行一次短小探索检查。

### Fixed

- 预检失败、创建失败、执行异常、超时或清理重试时可能出现假玩家残留、
  重复终态或消息顺序矛盾的问题。
- 低状态邻区块、出生点玩家占位、射线临时被玩家截获、目标方块类型改变和
  同名并发执行可能被错误接受的问题。
- 保存目标已加载但出生范围或射线路径跨入卸载邻区块时，在拒绝前读取世界
  状态的问题。

### Migration

- `1.0.0` 配置不会被删除；首次触发返回 `RELAY_REQUIRES_RESAVE`。
- 在正确装置旁用原名称重新执行一次 `/pearlrelay save` 即可升级为 v2。

### Validation

- Java 25 clean build、47 个 JUnit 与 21 个 Fabric 服务端 GameTest。
- 开发与生产客户端场景分别连续通过 20 次，覆盖真实客户端投珠、原版滞留
  装置释放、Pearl Relay 触发传送和玩家可见假人生命周期。
- Java 25 XVFB 工作流核对候选提交、被测发布 JAR SHA-256、日志、截图和
  上传产物。
- 发布 JAR 不包含测试类、测试夹具或 gametest 元数据；最终候选证据见
  `tasks/test-results/v1.1.0.md`。

## [1.1.0-rc.2] - 2026-07-28

### Added

- 增加 Java 25 GitHub Actions 质量门，在每次 push 和 pull request 中运行
  JUnit、Fabric GameTest 与构建，并上传发布 JAR、SHA-256 和测试证据。
- 增加通过真实 Brigadier 命令树执行的双 UUID GameTest，覆盖
  `test/save/list/fire/remove`、补全隔离、旧配置重存、0/他人/1/多颗珍珠
  和重复触发。
- 增加拒绝路径的日志、消息、假人数量、交互次数、目标区块票据和加载状态
  断言。

### Changed

- 预检现在先确认保存目标、假人出生范围和射线路径的所有相关区块均可实体
  tick，再读取方块状态、检查碰撞、射线或扫描实体。

### Fixed

- 保存目标区块已加载、但假人出生位置或射线路径跨入未加载邻区块时，
  预检会在最终拒绝前先读取已加载目标方块的问题。

### Validation

- 47 个 JUnit 测试和 21 个 Fabric GameTest。
- 真实未加载目标区块在命令拒绝前后保持无区块对象、无新增票据、无假人。
- 该 RC 发布时仍保留真人验收门槛；稳定版已用后续客户端 E2E 与生产 JAR
  证据替代。

## [1.1.0-rc.1] - 2026-07-27

### Added

- 在创建 Carpet 假人前，对维度、已加载区块、出生碰撞、目标类型、目标可达性和珍珠所有权执行完整预检。
- 为每个中继保存目标方块坐标和注册表 ID，并将配置格式升级为 `schemaVersion: 2`。
- 要求目标方块所在区块至少存在一颗属于命令执行玩家的末影珍珠；允许多颗，不选择或操作具体珍珠。
- 增加稳定的错误码、玩家提示、执行关联 ID 和可查询的 `event=relay_fire` 生命周期日志。
- 增加带超时和幂等清理的执行状态机，覆盖生成失败、执行异常、并发触发和服务器停止。
- 增加 JUnit、Fabric GameTest、配置兼容性样例和隔离服务端验证流程。
- 在 Fabric 元数据中声明 Java 25、Fabric API `0.152.1+26.2` 和 Carpet `26.2` 的已验证最低版本。

### Changed

- `/pearlrelay save` 现在要求出生位置与射线路径区块已加载，且射线在生存交互距离内准确命中目标方块。
- `/pearlrelay fire` 现在只在全部预检通过后创建假人。
- 目标方块的可变状态不参与指纹；相同方块类型的开关或供电状态变化仍然有效。
- 未加载的相关区块会直接拒绝；模组不会自动申请区块加载。
- 假人激活流程明确为 `spawn -> lookAt -> use once -> cleanup`。

### Fixed

- 预检失败后仍可能创建假人或留下假人的风险。
- 仅以 `getChunkNow` 判断时，可能把票据周围完整但不可实体 tick 的邻区块误判为可用的问题。
- 玩家占据假人出生点或临时进入 USE 射线时，Carpet 可能点击玩家而没有点击目标方块，但生命周期仍报告完成的问题。
- 同一生成假人名的并发执行可能重叠的问题。
- 异常、生成超时或清理重试路径缺少唯一终态的问题。
- Carpet 同步创建失败时，终态消息可能早于命令的“已排队”消息，导致玩家看到矛盾顺序的问题。
- 失败信息不精确、服务端无法关联一次触发完整生命周期的问题。

### Migration

- `1.0.0` 配置继续可读和可列出，但缺少目标指纹。
- 触发旧中继会返回 `RELAY_REQUIRES_RESAVE`。
- 玩家需要对旧名称重新执行一次 `/pearlrelay save`，新配置会覆盖并升级原条目。

### Known limitations

- 本模组只能证明假人已生成、瞄准、执行一次使用动作并退出，不能通用证明任意红石装置最终释放了珍珠或完成传送。
- 珍珠检查范围严格等于目标方块所在区块；跨区块装置需要保证玩家珍珠位于该区块。
- `fireRaw` 是开发排查指令，只做结构与执行安全检查，不做按玩家珍珠所有权检查。
- 权限、冷却、GUI、自动区块加载和直接珍珠控制不在 `1.1` 范围内。
- `1.1.0-rc.1` 仍需真实玩家完成珍珠装置传送与双账号隔离验收。

## [1.0.0] - 2026-06-24

### Added

- `/pearlrelay test`
- `/pearlrelay fireRaw`
- 按玩家 UUID 隔离的 `/pearlrelay save|list|fire|remove`
- 基于 Carpet 假人的出生、看向、单次右键和自动退出流程

### Historical note

- 仓库没有既有 `v1.0.0` 标签。提交 `306e9bf` 的源码声明版本为 `1.0.0`，但在核实实际部署 JAR 的来源和校验和前，不补建可能误导的历史标签。
